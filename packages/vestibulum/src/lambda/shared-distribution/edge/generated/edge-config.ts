// PLACEHOLDER — regenerated by vestibulum-cdk's build-bundles script at synth time.
// Sentinel values for typecheck + tests.
//
// The construct (P2b) overwrites this file with concrete values derived from
// SharedDistributionIdentity props. The SHA-256 of this file participates in
// the bundle lock manifest; changes here require explicit reviewer ack.
//
// Lambda@Edge does not support environment variables, so config MUST be baked
// into the source — see review finding B4.

export const TENANT_PARENT = 'tenants.example.com';
export const TENANT_PATTERN = /^[a-z][a-z0-9-]{1,61}[a-z0-9]$/;
export const POOL_ISSUER = 'https://cognito-idp.eu-central-1.amazonaws.com/eu-central-1_PLACEHOLDER';
export const JWKS_URL = `${POOL_ISSUER}/.well-known/jwks.json`;
export const JWKS_TTL_MS = 900_000;
