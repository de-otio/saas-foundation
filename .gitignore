# doc-search index
.doc-search-index
# doc-search index
.mcp.json

# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Test coverage
coverage/

# Lambda bundles (generated at build time)
lambda-bundles/

# SAML test fixtures — regenerated on every run by the vitest globalSetup
# (packages/vestibulum/test/fixtures/saml/build-fixtures.ts), signed with a
# freshly-minted key, so the committed copies only churn. The generator and
# its .pem inputs are the source of truth, not these artifacts.
packages/vestibulum/test/fixtures/saml/*.xml

# Environment files
.env
.env.*
!.env.example

# Secrets / credentials
credentials*
*.pem
id_rsa
id_ed25519

# CDK output
cdk.out/

# Editor
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# npm
npm-debug.log*

# Temporary files
tmp/
temp/
plans/